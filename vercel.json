{
  "version": 2,
  "buildCommand": "cd client && npm cache clean --force && rm -rf node_modules package-lock.json && npm install --include=dev && npm run build",
  "outputDirectory": "client/build",
  "installCommand": "echo 'Skipping root install'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "GENERATE_SOURCEMAP": "false"
  }
}
